services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: takeme
      POSTGRES_PASSWORD: takeme
      POSTGRES_DB: takeme
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: